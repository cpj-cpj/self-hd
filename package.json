{
  "name": "self-hd",
  "version": "1.0.0",
  "description": "SIT223 7.3HD Jenkins DevOps pipeline demo service.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "build": "node scripts/build.js",
    "test": "jest --runInBand --coverage",
    "lint": "eslint src test scripts",
    "quality": "npm run lint && npm test -- --coverage=false",
    "security:audit": "npm audit --audit-level=high"
  },
  "keywords": [
    "jenkins",
    "docker",
    "devops",
    "sit223"
  ],
  "license": "MIT",
  "dependencies": {
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "nodemon": "^3.1.4",
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}

